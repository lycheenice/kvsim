kvsim 是一个新的仓库

我想写一个软件，可以完成大模型推理过程中 kvcache 用量估算，以及 offload / kv transfer 带宽的估算，后续可能新增其他功能。请先充分调研已有方案，完成架构设计文档 v0.1，基于这个版本我们再进一步讨论。
v0.1 需要满足以下功能：
1. 支持 kvcache 显存用量的估算（根据序列长度），涵盖主流开源大模型以及自定义模型，自定义模型可以自动读取模型架构文件完成 kvcache 的计算，模型架构文件参考：https://huggingface.co/zai-org/GLM-4.7/blob/main/config.json
2. 支持命令行展示和前端页面展示，第一个版本可以先实现命令行，后续版本再实现web 页面，做到前后端分离
3. 在 kvcache 显存占用量估算的基础上需要支持 pd 分离时带宽占用的估算，要求在逐层异步传输的时候下一层的计算能掩盖这一层的延迟，计算时间可以根据峰值算力的 50% 进行估计，attn+ffn 的计算量可以通过模型参数算出来
4. 在上述基础上支持 kvcache offload 的带宽计算，但这块如何计算带宽我还没有想好，请做一些调研工作作为下一步讨论的基础

以上，请完成设计文档，我们再进一步讨论。